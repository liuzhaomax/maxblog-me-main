syntax="proto3";

option go_package="/pb";

service DemoService {
    rpc GetDemoById(IdRequest) returns (DemoRes);
}

message IdRequest {
    uint32 id=1;
}

message DemoRes {
    int32 id=1;
    string mobile=2;
}